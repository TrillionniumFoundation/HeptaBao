name: HeptaBao V2.4 migration inventory r3

on:
  push:
    branches:
      - ops/v24-migration-inventory-r3-20260909

permissions:
  contents: write

concurrency:
  group: heptabao-v24-migration-inventory-r3
  cancel-in-progress: false

env:
  EXACT_PARENT: 633848024e5315f3def23c035a1dec53f83a62b3
  INPUT_BRANCH: codex/openbao-replacement-v2.4-migration-journal-source-20260909
  OUTPUT_BRANCH: codex/openbao-replacement-v2.4-migration-source-r3-20260909

jobs:
  harden:
    runs-on: ubuntu-24.04
    timeout-minutes: 150
    steps:
      - name: Checkout controller
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Wait for migration journal source
        shell: bash
        run: |
          set -euo pipefail
          deadline=$((SECONDS + 7200))
          while [ "$SECONDS" -lt "$deadline" ]; do
            git fetch --no-tags origin "+refs/heads/$INPUT_BRANCH:refs/remotes/origin/$INPUT_BRANCH" || true
            if git show-ref --verify --quiet "refs/remotes/origin/$INPUT_BRANCH"; then
              source_sha="$(git rev-parse "refs/remotes/origin/$INPUT_BRANCH")"
              git merge-base --is-ancestor "$EXACT_PARENT" "$source_sha"
              printf 'SOURCE_SHA=%s\n' "$source_sha" >> "$GITHUB_ENV"
              exit 0
            fi
            sleep 30
          done
          echo 'migration journal source did not materialize' >&2
          exit 1

      - name: Add complete migration inventory in isolated worktree
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install 1.98.0 --profile minimal --component rustfmt --component clippy
          work="$RUNNER_TEMP/heptabao-migration-inventory-r3"
          git worktree add --detach "$work" "$SOURCE_SHA"
          (
            cd "$work"
            python - <<'PY'
          from pathlib import Path

          path=Path('crates/heptabao-migration-journal/src/lib.rs')
          text=path.read_text(encoding='utf-8')
          if 'pub enum MigrationObjectKindV24' in text:
              raise SystemExit('migration inventory already exists')
          text=text.replace('use std::collections::BTreeMap;\n','use std::collections::{BTreeMap, BTreeSet};\n',1)
          addition=r'''

const MAX_MIGRATION_OBJECTS_V2_4: usize = 1_000_000;
const MAX_OBJECT_DEPENDENCIES_V2_4: usize = 1024;

#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum MigrationObjectKindV24 {
    Namespace,
    Policy,
    AuthMount,
    SecretMount,
    IdentityEntity,
    IdentityGroup,
    IdentityAlias,
    TokenRole,
    TransitKey,
    PkiIssuer,
    PkiRole,
    DatabaseRole,
    DynamicLease,
    AuditDevice,
    SealMetadata,
    RaftMetadata,
}

impl MigrationObjectKindV24 {
    pub const fn all_required_for_complete_profile() -> [Self; 16] {
        [
            Self::Namespace,
            Self::Policy,
            Self::AuthMount,
            Self::SecretMount,
            Self::IdentityEntity,
            Self::IdentityGroup,
            Self::IdentityAlias,
            Self::TokenRole,
            Self::TransitKey,
            Self::PkiIssuer,
            Self::PkiRole,
            Self::DatabaseRole,
            Self::DynamicLease,
            Self::AuditDevice,
            Self::SealMetadata,
            Self::RaftMetadata,
        ]
    }
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub struct MigrationObjectV24 {
    pub object_id: MigrationObjectId,
    pub kind: MigrationObjectKindV24,
    pub source_digest: [u8; 32],
    pub dependencies: BTreeSet<MigrationObjectId>,
}

impl MigrationObjectV24 {
    pub fn new(
        object_id: MigrationObjectId,
        kind: MigrationObjectKindV24,
        source_digest: [u8; 32],
        dependencies: BTreeSet<MigrationObjectId>,
    ) -> Result<Self, MigrationInventoryErrorV24> {
        if source_digest == [0; 32]
            || dependencies.len() > MAX_OBJECT_DEPENDENCIES_V2_4
            || dependencies.contains(&object_id)
        {
            return Err(MigrationInventoryErrorV24::InvalidObject);
        }
        Ok(Self {
            object_id,
            kind,
            source_digest,
            dependencies,
        })
    }
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub struct MigrationInventoryV24 {
    objects: BTreeMap<MigrationObjectId, MigrationObjectV24>,
    required_kinds: BTreeSet<MigrationObjectKindV24>,
    topological_order: Vec<MigrationObjectId>,
}

impl MigrationInventoryV24 {
    pub fn complete_profile(
        objects: impl IntoIterator<Item = MigrationObjectV24>,
    ) -> Result<Self, MigrationInventoryErrorV24> {
        Self::new(
            objects,
            MigrationObjectKindV24::all_required_for_complete_profile(),
        )
    }

    pub fn new(
        objects: impl IntoIterator<Item = MigrationObjectV24>,
        required_kinds: impl IntoIterator<Item = MigrationObjectKindV24>,
    ) -> Result<Self, MigrationInventoryErrorV24> {
        let required_kinds = required_kinds.into_iter().collect::<BTreeSet<_>>();
        if required_kinds.is_empty() {
            return Err(MigrationInventoryErrorV24::EmptyRequiredKinds);
        }
        let mut by_id = BTreeMap::new();
        for object in objects {
            if by_id.len() >= MAX_MIGRATION_OBJECTS_V2_4 {
                return Err(MigrationInventoryErrorV24::InventoryTooLarge);
            }
            if by_id.insert(object.object_id.clone(), object).is_some() {
                return Err(MigrationInventoryErrorV24::DuplicateObject);
            }
        }
        if by_id.is_empty() {
            return Err(MigrationInventoryErrorV24::EmptyInventory);
        }
        for object in by_id.values() {
            if object
                .dependencies
                .iter()
                .any(|dependency| !by_id.contains_key(dependency))
            {
                return Err(MigrationInventoryErrorV24::MissingDependency);
            }
        }
        let observed_kinds = by_id.values().map(|object| object.kind).collect::<BTreeSet<_>>();
        if !required_kinds.is_subset(&observed_kinds) {
            return Err(MigrationInventoryErrorV24::MissingRequiredKind);
        }
        let topological_order = migration_topological_order_v2_4(&by_id)?;
        Ok(Self {
            objects: by_id,
            required_kinds,
            topological_order,
        })
    }

    pub fn objects(&self) -> impl Iterator<Item = &MigrationObjectV24> {
        self.topological_order
            .iter()
            .filter_map(|object_id| self.objects.get(object_id))
    }

    pub fn topological_order(&self) -> &[MigrationObjectId] {
        &self.topological_order
    }

    pub fn required_kinds(&self) -> &BTreeSet<MigrationObjectKindV24> {
        &self.required_kinds
    }

    pub fn len(&self) -> usize {
        self.objects.len()
    }

    pub fn is_empty(&self) -> bool {
        self.objects.is_empty()
    }
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub enum MigrationInventoryErrorV24 {
    InvalidObject,
    EmptyRequiredKinds,
    EmptyInventory,
    InventoryTooLarge,
    DuplicateObject,
    MissingDependency,
    MissingRequiredKind,
    DependencyCycle,
}

impl fmt::Display for MigrationInventoryErrorV24 {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result {
        formatter.write_str(match self {
            Self::InvalidObject => "migration inventory object is invalid",
            Self::EmptyRequiredKinds => "migration inventory required-kind set is empty",
            Self::EmptyInventory => "migration inventory is empty",
            Self::InventoryTooLarge => "migration inventory exceeds its bound",
            Self::DuplicateObject => "migration inventory object is duplicated",
            Self::MissingDependency => "migration inventory dependency is missing",
            Self::MissingRequiredKind => "migration inventory omits a required object kind",
            Self::DependencyCycle => "migration inventory contains a dependency cycle",
        })
    }
}

impl Error for MigrationInventoryErrorV24 {}

fn migration_topological_order_v2_4(
    objects: &BTreeMap<MigrationObjectId, MigrationObjectV24>,
) -> Result<Vec<MigrationObjectId>, MigrationInventoryErrorV24> {
    let mut remaining = objects
        .iter()
        .map(|(object_id, object)| (object_id.clone(), object.dependencies.clone()))
        .collect::<BTreeMap<_, _>>();
    let mut order = Vec::with_capacity(objects.len());
    while !remaining.is_empty() {
        let ready = remaining
            .iter()
            .filter(|(_, dependencies)| dependencies.is_empty())
            .map(|(object_id, _)| object_id.clone())
            .collect::<Vec<_>>();
        if ready.is_empty() {
            return Err(MigrationInventoryErrorV24::DependencyCycle);
        }
        for object_id in &ready {
            remaining.remove(object_id);
            order.push(object_id.clone());
        }
        for dependencies in remaining.values_mut() {
            for object_id in &ready {
                dependencies.remove(object_id);
            }
        }
    }
    Ok(order)
}

#[cfg(test)]
mod migration_inventory_v2_4_tests {
    use super::*;

    fn object(
        id: &str,
        kind: MigrationObjectKindV24,
        dependencies: &[&str],
    ) -> MigrationObjectV24 {
        MigrationObjectV24::new(
            MigrationObjectId::parse(id).unwrap(),
            kind,
            [id.as_bytes()[0]; 32],
            dependencies
                .iter()
                .map(|value| MigrationObjectId::parse(*value).unwrap())
                .collect(),
        )
        .unwrap()
    }

    #[test]
    fn deterministic_dependency_order_is_enforced() {
        let namespace = object("namespace/root", MigrationObjectKindV24::Namespace, &[]);
        let policy = object(
            "policy/default",
            MigrationObjectKindV24::Policy,
            &["namespace/root"],
        );
        let mount = object(
            "mount/kv",
            MigrationObjectKindV24::SecretMount,
            &["namespace/root", "policy/default"],
        );
        let inventory = MigrationInventoryV24::new(
            [mount, policy, namespace],
            [
                MigrationObjectKindV24::Namespace,
                MigrationObjectKindV24::Policy,
                MigrationObjectKindV24::SecretMount,
            ],
        )
        .unwrap();
        let order = inventory
            .topological_order()
            .iter()
            .map(MigrationObjectId::as_str)
            .collect::<Vec<_>>();
        assert_eq!(order, ["namespace/root", "policy/default", "mount/kv"]);
    }

    #[test]
    fn missing_kind_dependency_and_cycle_fail_closed() {
        let namespace = object("namespace/root", MigrationObjectKindV24::Namespace, &[]);
        assert_eq!(
            MigrationInventoryV24::new(
                [namespace.clone()],
                [MigrationObjectKindV24::Namespace, MigrationObjectKindV24::Policy],
            ),
            Err(MigrationInventoryErrorV24::MissingRequiredKind)
        );
        let missing = object(
            "policy/default",
            MigrationObjectKindV24::Policy,
            &["namespace/missing"],
        );
        assert_eq!(
            MigrationInventoryV24::new([namespace.clone(), missing], [MigrationObjectKindV24::Namespace]),
            Err(MigrationInventoryErrorV24::MissingDependency)
        );
        let first = object("cycle/first", MigrationObjectKindV24::Policy, &["cycle/second"]);
        let second = object("cycle/second", MigrationObjectKindV24::Policy, &["cycle/first"]);
        assert_eq!(
            MigrationInventoryV24::new([first, second], [MigrationObjectKindV24::Policy]),
            Err(MigrationInventoryErrorV24::DependencyCycle)
        );
    }

    #[test]
    fn complete_profile_enumerates_all_required_object_kinds() {
        let objects = MigrationObjectKindV24::all_required_for_complete_profile()
            .into_iter()
            .enumerate()
            .map(|(index, kind)| {
                object(
                    &format!("kind/object-{index}"),
                    kind,
                    &[],
                )
            })
            .collect::<Vec<_>>();
        let inventory = MigrationInventoryV24::complete_profile(objects).unwrap();
        assert_eq!(inventory.required_kinds().len(), 16);
    }
}
'''
          path.write_text(text.rstrip()+addition+'\n',encoding='utf-8')

          guide=Path('docs/modules/heptabao-migration-journal.md')
          g=guide.read_text(encoding='utf-8')
          block='''
## V2.4 complete object inventory

`MigrationInventoryV24` requires an explicit bounded object set, validates every dependency, rejects duplicate IDs and cycles, produces a deterministic topological order, and can require the complete profile covering namespace, policy, auth/secret mounts, identity, token roles, Transit, PKI, database roles, dynamic leases, audit devices, seal metadata and Raft metadata. This proves inventory completeness and ordering, not semantic correctness of each object adapter; source/target fixtures and cutover evidence remain required.
'''
          if '## V2.4 complete object inventory' not in g:
              guide.write_text(g.rstrip()+block,encoding='utf-8')
          Path('tests/repository/test_migration_inventory_v2_4.py').write_text('''from pathlib import Path\nimport unittest\n\n\nclass MigrationInventoryV24Tests(unittest.TestCase):\n    def test_all_object_kinds_and_dependency_cycle_rejection_exist(self):\n        source=Path("crates/heptabao-migration-journal/src/lib.rs").read_text(encoding="utf-8")\n        for marker in ("MigrationObjectKindV24", "all_required_for_complete_profile", "MissingRequiredKind", "MissingDependency", "DependencyCycle", "migration_topological_order_v2_4"):\n            self.assertIn(marker,source)\n\n    def test_adapter_and_cutover_boundary_is_explicit(self):\n        guide=Path("docs/modules/heptabao-migration-journal.md").read_text(encoding="utf-8")\n        self.assertIn("not semantic correctness of each object adapter",guide)\n        self.assertIn("cutover evidence remain required",guide)\n\n\nif __name__ == "__main__":\n    unittest.main()\n''',encoding='utf-8')
          PY
            cargo +1.98.0 fmt --all
            cargo +1.98.0 test --locked -p heptabao-migration-journal --all-targets
            cargo +1.98.0 clippy --locked -p heptabao-migration-journal --all-targets -- -D warnings
            python -m unittest \
              tests.repository.test_migration_journal_v2_4 \
              tests.repository.test_migration_inventory_v2_4
            git config user.name 'HeptaBao V2.4 Migration Inventory Hardening'
            git config user.email '102159240+ProfHepta@users.noreply.github.com'
            git add -A
            git commit -m 'feat(migration): require complete object inventory and dependency order'
            new_sha="$(git rev-parse HEAD)"
            if git ls-remote --exit-code origin "refs/heads/$OUTPUT_BRANCH" >/dev/null 2>&1; then
              observed="$(git ls-remote origin "refs/heads/$OUTPUT_BRANCH" | cut -f1)"
              test "$observed" = "$new_sha"
            else
              git push --force-with-lease="refs/heads/$OUTPUT_BRANCH:" origin "$new_sha:refs/heads/$OUTPUT_BRANCH"
            fi
          )
          git worktree remove --force "$work"
