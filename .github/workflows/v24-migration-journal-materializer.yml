name: HeptaBao V2.4 migration journal materializer

on:
  push:
    branches:
      - ops/v24-migration-journal-r1-20260909

permissions:
  contents: write

concurrency:
  group: heptabao-v24-migration-journal-r1
  cancel-in-progress: false

env:
  EXACT_PARENT: 633848024e5315f3def23c035a1dec53f83a62b3
  OUTPUT_BRANCH: codex/openbao-replacement-v2.4-migration-journal-20260909

jobs:
  materialize:
    runs-on: ubuntu-24.04
    timeout-minutes: 100
    steps:
      - name: Checkout exact staging head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Materialize durable migration journal package
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD^)" = "$EXACT_PARENT"
          python - <<'PY'
          from pathlib import Path
          import re

          crate = Path('crates/heptabao-migration-journal')
          (crate / 'src').mkdir(parents=True, exist_ok=True)
          (crate / 'Cargo.toml').write_text('''[package]
name = "heptabao-migration-journal"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
ring = "0.17.14"
''', encoding='utf-8')
          (crate / 'src/lib.rs').write_text(r'''#![forbid(unsafe_code)]
#![deny(missing_debug_implementations)]

//! Durable, fail-closed migration transaction journal.
//!
//! The journal provides an authenticated append-only state machine for moving
//! one logical object at a time. A commit whose client-visible outcome is
//! uncertain can only transition to committed after an explicit reconciliation
//! proof binds the observed target digest. Torn tails are repaired under the
//! single-writer lock; authenticated-prefix corruption is rejected.

use std::collections::BTreeMap;
use std::error::Error;
use std::fmt;
use std::fs::{self, File, OpenOptions};
use std::io::{self, Read, Seek, SeekFrom, Write};
use std::path::{Component, Path, PathBuf};

use ring::{digest, hmac};

const MAGIC: &[u8; 5] = b"HBMJ1";
const MAX_RECORD_BYTES: usize = 64 * 1024;
const MAX_OBJECT_ID_BYTES: usize = 512;
const TAG_BYTES: usize = 32;

#[derive(Clone, Eq, Ord, PartialEq, PartialOrd)]
pub struct MigrationObjectId(String);

impl MigrationObjectId {
    pub fn parse(value: impl Into<String>) -> Result<Self, JournalError> {
        let value = value.into();
        if value.is_empty()
            || value.len() > MAX_OBJECT_ID_BYTES
            || value.starts_with('/')
            || value.ends_with('/')
            || value.contains("//")
            || value.split('/').any(|segment| {
                segment.is_empty()
                    || segment == "."
                    || segment == ".."
                    || !segment.bytes().all(|byte| {
                        byte.is_ascii_alphanumeric()
                            || matches!(byte, b'-' | b'_' | b'.' | b'/' | b':')
                    })
            })
        {
            return Err(JournalError::InvalidObjectId);
        }
        Ok(Self(value))
    }

    pub fn as_str(&self) -> &str {
        &self.0
    }
}

impl fmt::Debug for MigrationObjectId {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result {
        formatter.debug_tuple("MigrationObjectId").field(&self.0).finish()
    }
}

impl fmt::Display for MigrationObjectId {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result {
        formatter.write_str(&self.0)
    }
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
#[repr(u8)]
pub enum MigrationPhase {
    Planned = 1,
    Staged = 2,
    Verified = 3,
    CommitPending = 4,
    Committed = 5,
    RollbackPending = 6,
    RolledBack = 7,
}

impl MigrationPhase {
    fn decode(value: u8) -> Result<Self, JournalError> {
        match value {
            1 => Ok(Self::Planned),
            2 => Ok(Self::Staged),
            3 => Ok(Self::Verified),
            4 => Ok(Self::CommitPending),
            5 => Ok(Self::Committed),
            6 => Ok(Self::RollbackPending),
            7 => Ok(Self::RolledBack),
            _ => Err(JournalError::InvalidRecord),
        }
    }
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub struct MigrationRecord {
    pub sequence: u64,
    pub operation_id: [u8; 16],
    pub object_id: MigrationObjectId,
    pub phase: MigrationPhase,
    pub source_digest: [u8; 32],
    pub target_digest: [u8; 32],
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub struct ReconciliationProof {
    pub committed: bool,
    pub observed_target_digest: [u8; 32],
}

#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub enum JournalError {
    InvalidRoot,
    InvalidKey,
    InvalidObjectId,
    InvalidRecord,
    InvalidTransition,
    WriterBusy,
    Tampered,
    SequenceOverflow,
    OutcomeUnknown,
    Io,
}

impl fmt::Display for JournalError {
    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result {
        formatter.write_str(match self {
            Self::InvalidRoot => "migration journal root is invalid",
            Self::InvalidKey => "migration journal authentication key is invalid",
            Self::InvalidObjectId => "migration object identifier is invalid",
            Self::InvalidRecord => "migration journal record is invalid",
            Self::InvalidTransition => "migration state transition is invalid",
            Self::WriterBusy => "migration journal already has a writer",
            Self::Tampered => "migration journal authentication failed",
            Self::SequenceOverflow => "migration journal sequence overflow",
            Self::OutcomeUnknown => "migration journal write outcome is unknown",
            Self::Io => "migration journal I/O failed",
        })
    }
}

impl Error for JournalError {}

#[derive(Debug)]
pub struct MigrationJournal {
    root: PathBuf,
    journal_path: PathBuf,
    lock_path: PathBuf,
    journal: File,
    _lock: File,
    key: [u8; 32],
    next_sequence: u64,
    previous_tag: [u8; TAG_BYTES],
    latest: BTreeMap<MigrationObjectId, MigrationRecord>,
}

impl MigrationJournal {
    pub fn open(root: impl AsRef<Path>, key: [u8; 32]) -> Result<Self, JournalError> {
        if key == [0; 32] {
            return Err(JournalError::InvalidKey);
        }
        let root = root.as_ref().to_path_buf();
        validate_root(&root)?;
        create_private_root(&root)?;
        let lock_path = root.join("writer.lock");
        let journal_path = root.join("migration.journal");
        let lock = create_lock(&lock_path)?;
        let mut journal = open_journal(&journal_path)?;
        let decoded = recover_records(&mut journal, key)?;
        if decoded.valid_bytes < journal.metadata().map_err(|_| JournalError::Io)?.len() {
            journal
                .set_len(decoded.valid_bytes)
                .and_then(|()| journal.sync_all())
                .map_err(|_| JournalError::Io)?;
        }
        journal
            .seek(SeekFrom::End(0))
            .map_err(|_| JournalError::Io)?;
        let next_sequence = decoded
            .last_sequence
            .checked_add(1)
            .ok_or(JournalError::SequenceOverflow)?;
        Ok(Self {
            root,
            journal_path,
            lock_path,
            journal,
            _lock: lock,
            key,
            next_sequence,
            previous_tag: decoded.previous_tag,
            latest: decoded.latest,
        })
    }

    pub fn root(&self) -> &Path {
        &self.root
    }

    pub fn journal_path(&self) -> &Path {
        &self.journal_path
    }

    pub fn latest(&self, object_id: &MigrationObjectId) -> Option<&MigrationRecord> {
        self.latest.get(object_id)
    }

    pub fn begin(
        &mut self,
        operation_id: [u8; 16],
        object_id: MigrationObjectId,
        source_digest: [u8; 32],
    ) -> Result<MigrationRecord, JournalError> {
        if operation_id == [0; 16] || source_digest == [0; 32] {
            return Err(JournalError::InvalidRecord);
        }
        if let Some(record) = self.latest.get(&object_id) {
            if record.operation_id == operation_id
                && record.phase == MigrationPhase::Planned
                && record.source_digest == source_digest
            {
                return Ok(record.clone());
            }
            if record.phase != MigrationPhase::RolledBack {
                return Err(JournalError::InvalidTransition);
            }
        }
        self.append(MigrationRecord {
            sequence: 0,
            operation_id,
            object_id,
            phase: MigrationPhase::Planned,
            source_digest,
            target_digest: [0; 32],
        })
    }

    pub fn mark_staged(
        &mut self,
        object_id: &MigrationObjectId,
        target_digest: [u8; 32],
    ) -> Result<MigrationRecord, JournalError> {
        if target_digest == [0; 32] {
            return Err(JournalError::InvalidRecord);
        }
        self.transition(object_id, MigrationPhase::Planned, MigrationPhase::Staged, target_digest)
    }

    pub fn mark_verified(
        &mut self,
        object_id: &MigrationObjectId,
    ) -> Result<MigrationRecord, JournalError> {
        let current = self.require(object_id, MigrationPhase::Staged)?.clone();
        self.append(MigrationRecord {
            sequence: 0,
            phase: MigrationPhase::Verified,
            ..current
        })
    }

    pub fn mark_commit_pending(
        &mut self,
        object_id: &MigrationObjectId,
    ) -> Result<MigrationRecord, JournalError> {
        let current = self.require(object_id, MigrationPhase::Verified)?.clone();
        self.append(MigrationRecord {
            sequence: 0,
            phase: MigrationPhase::CommitPending,
            ..current
        })
    }

    pub fn reconcile_commit(
        &mut self,
        object_id: &MigrationObjectId,
        proof: ReconciliationProof,
    ) -> Result<MigrationRecord, JournalError> {
        let current = self.require(object_id, MigrationPhase::CommitPending)?.clone();
        if !proof.committed || proof.observed_target_digest != current.target_digest {
            return Err(JournalError::InvalidTransition);
        }
        self.append(MigrationRecord {
            sequence: 0,
            phase: MigrationPhase::Committed,
            ..current
        })
    }

    pub fn mark_rollback_pending(
        &mut self,
        object_id: &MigrationObjectId,
    ) -> Result<MigrationRecord, JournalError> {
        let current = self
            .latest
            .get(object_id)
            .ok_or(JournalError::InvalidTransition)?
            .clone();
        if !matches!(
            current.phase,
            MigrationPhase::Planned
                | MigrationPhase::Staged
                | MigrationPhase::Verified
                | MigrationPhase::CommitPending
        ) {
            return Err(JournalError::InvalidTransition);
        }
        self.append(MigrationRecord {
            sequence: 0,
            phase: MigrationPhase::RollbackPending,
            ..current
        })
    }

    pub fn reconcile_rollback(
        &mut self,
        object_id: &MigrationObjectId,
        target_absent: bool,
    ) -> Result<MigrationRecord, JournalError> {
        let current = self.require(object_id, MigrationPhase::RollbackPending)?.clone();
        if !target_absent {
            return Err(JournalError::InvalidTransition);
        }
        self.append(MigrationRecord {
            sequence: 0,
            phase: MigrationPhase::RolledBack,
            ..current
        })
    }

    fn transition(
        &mut self,
        object_id: &MigrationObjectId,
        expected: MigrationPhase,
        phase: MigrationPhase,
        target_digest: [u8; 32],
    ) -> Result<MigrationRecord, JournalError> {
        let current = self.require(object_id, expected)?.clone();
        self.append(MigrationRecord {
            sequence: 0,
            phase,
            target_digest,
            ..current
        })
    }

    fn require(
        &self,
        object_id: &MigrationObjectId,
        expected: MigrationPhase,
    ) -> Result<&MigrationRecord, JournalError> {
        let current = self
            .latest
            .get(object_id)
            .ok_or(JournalError::InvalidTransition)?;
        if current.phase != expected {
            return Err(JournalError::InvalidTransition);
        }
        Ok(current)
    }

    fn append(&mut self, mut record: MigrationRecord) -> Result<MigrationRecord, JournalError> {
        record.sequence = self.next_sequence;
        let body = encode_body(&record, self.previous_tag)?;
        let signing_key = hmac::Key::new(hmac::HMAC_SHA256, &self.key);
        let tag = hmac::sign(&signing_key, &body);
        let mut frame = Vec::with_capacity(4 + body.len() + TAG_BYTES);
        let frame_len = body
            .len()
            .checked_add(TAG_BYTES)
            .and_then(|value| u32::try_from(value).ok())
            .ok_or(JournalError::InvalidRecord)?;
        frame.extend_from_slice(&frame_len.to_be_bytes());
        frame.extend_from_slice(&body);
        frame.extend_from_slice(tag.as_ref());
        if self.journal.write_all(&frame).is_err()
            || self.journal.flush().is_err()
            || self.journal.sync_all().is_err()
        {
            return Err(JournalError::OutcomeUnknown);
        }
        self.previous_tag.copy_from_slice(tag.as_ref());
        self.next_sequence = self
            .next_sequence
            .checked_add(1)
            .ok_or(JournalError::SequenceOverflow)?;
        self.latest.insert(record.object_id.clone(), record.clone());
        Ok(record)
    }
}

impl Drop for MigrationJournal {
    fn drop(&mut self) {
        self.key.fill(0);
        let _ = fs::remove_file(&self.lock_path);
    }
}

#[derive(Debug)]
struct DecodedJournal {
    valid_bytes: u64,
    last_sequence: u64,
    previous_tag: [u8; TAG_BYTES],
    latest: BTreeMap<MigrationObjectId, MigrationRecord>,
}

fn recover_records(journal: &mut File, key: [u8; 32]) -> Result<DecodedJournal, JournalError> {
    journal.seek(SeekFrom::Start(0)).map_err(|_| JournalError::Io)?;
    let mut bytes = Vec::new();
    journal.read_to_end(&mut bytes).map_err(|_| JournalError::Io)?;
    let signing_key = hmac::Key::new(hmac::HMAC_SHA256, &key);
    let mut offset = 0_usize;
    let mut expected_sequence = 1_u64;
    let mut previous_tag = [0_u8; TAG_BYTES];
    let mut latest = BTreeMap::new();
    while offset < bytes.len() {
        if bytes.len() - offset < 4 {
            break;
        }
        let length = u32::from_be_bytes(bytes[offset..offset + 4].try_into().map_err(|_| JournalError::InvalidRecord)?) as usize;
        if length < TAG_BYTES || length > MAX_RECORD_BYTES || bytes.len() - offset - 4 < length {
            break;
        }
        let frame_start = offset + 4;
        let body_end = frame_start + length - TAG_BYTES;
        let frame_end = frame_start + length;
        let body = &bytes[frame_start..body_end];
        let tag = &bytes[body_end..frame_end];
        hmac::verify(&signing_key, body, tag).map_err(|_| JournalError::Tampered)?;
        let (record, recorded_previous) = decode_body(body)?;
        if record.sequence != expected_sequence || recorded_previous != previous_tag {
            return Err(JournalError::Tampered);
        }
        previous_tag.copy_from_slice(tag);
        latest.insert(record.object_id.clone(), record);
        expected_sequence = expected_sequence
            .checked_add(1)
            .ok_or(JournalError::SequenceOverflow)?;
        offset = frame_end;
    }
    Ok(DecodedJournal {
        valid_bytes: offset as u64,
        last_sequence: expected_sequence - 1,
        previous_tag,
        latest,
    })
}

fn encode_body(record: &MigrationRecord, previous_tag: [u8; TAG_BYTES]) -> Result<Vec<u8>, JournalError> {
    let id = record.object_id.as_str().as_bytes();
    let id_len = u16::try_from(id.len()).map_err(|_| JournalError::InvalidRecord)?;
    let mut body = Vec::with_capacity(5 + 8 + 16 + 1 + 2 + id.len() + 32 + 32 + TAG_BYTES);
    body.extend_from_slice(MAGIC);
    body.extend_from_slice(&record.sequence.to_be_bytes());
    body.extend_from_slice(&record.operation_id);
    body.push(record.phase as u8);
    body.extend_from_slice(&id_len.to_be_bytes());
    body.extend_from_slice(id);
    body.extend_from_slice(&record.source_digest);
    body.extend_from_slice(&record.target_digest);
    body.extend_from_slice(&previous_tag);
    if body.len() + TAG_BYTES > MAX_RECORD_BYTES {
        return Err(JournalError::InvalidRecord);
    }
    Ok(body)
}

fn decode_body(body: &[u8]) -> Result<(MigrationRecord, [u8; TAG_BYTES]), JournalError> {
    const FIXED_BEFORE_ID: usize = 5 + 8 + 16 + 1 + 2;
    const FIXED_AFTER_ID: usize = 32 + 32 + TAG_BYTES;
    if body.len() < FIXED_BEFORE_ID + FIXED_AFTER_ID || &body[..5] != MAGIC {
        return Err(JournalError::InvalidRecord);
    }
    let mut cursor = 5;
    let sequence = u64::from_be_bytes(body[cursor..cursor + 8].try_into().map_err(|_| JournalError::InvalidRecord)?);
    cursor += 8;
    let operation_id = body[cursor..cursor + 16].try_into().map_err(|_| JournalError::InvalidRecord)?;
    cursor += 16;
    let phase = MigrationPhase::decode(body[cursor])?;
    cursor += 1;
    let id_len = u16::from_be_bytes(body[cursor..cursor + 2].try_into().map_err(|_| JournalError::InvalidRecord)?) as usize;
    cursor += 2;
    if id_len == 0 || id_len > MAX_OBJECT_ID_BYTES || cursor + id_len + FIXED_AFTER_ID != body.len() {
        return Err(JournalError::InvalidRecord);
    }
    let object_id = MigrationObjectId::parse(
        std::str::from_utf8(&body[cursor..cursor + id_len])
            .map_err(|_| JournalError::InvalidRecord)?
            .to_owned(),
    )?;
    cursor += id_len;
    let source_digest = body[cursor..cursor + 32].try_into().map_err(|_| JournalError::InvalidRecord)?;
    cursor += 32;
    let target_digest = body[cursor..cursor + 32].try_into().map_err(|_| JournalError::InvalidRecord)?;
    cursor += 32;
    let previous_tag = body[cursor..cursor + TAG_BYTES].try_into().map_err(|_| JournalError::InvalidRecord)?;
    if sequence == 0 || operation_id == [0; 16] || source_digest == [0; 32] {
        return Err(JournalError::InvalidRecord);
    }
    Ok((MigrationRecord { sequence, operation_id, object_id, phase, source_digest, target_digest }, previous_tag))
}

fn validate_root(path: &Path) -> Result<(), JournalError> {
    if !path.is_absolute() {
        return Err(JournalError::InvalidRoot);
    }
    let mut current = PathBuf::new();
    for component in path.components() {
        match component {
            Component::Prefix(_) | Component::RootDir | Component::Normal(_) => {
                current.push(component.as_os_str());
                if current == Path::new("/") || !current.exists() {
                    continue;
                }
                let metadata = fs::symlink_metadata(&current).map_err(|_| JournalError::InvalidRoot)?;
                if metadata.file_type().is_symlink() {
                    return Err(JournalError::InvalidRoot);
                }
            }
            Component::CurDir | Component::ParentDir => return Err(JournalError::InvalidRoot),
        }
    }
    Ok(())
}

fn create_private_root(root: &Path) -> Result<(), JournalError> {
    if root.exists() {
        if !root.is_dir() {
            return Err(JournalError::InvalidRoot);
        }
    } else {
        #[cfg(unix)]
        {
            use std::os::unix::fs::DirBuilderExt;
            let mut builder = fs::DirBuilder::new();
            builder.recursive(true).mode(0o700);
            builder.create(root).map_err(|_| JournalError::Io)?;
        }
        #[cfg(not(unix))]
        fs::create_dir_all(root).map_err(|_| JournalError::Io)?;
    }
    #[cfg(unix)]
    {
        use std::os::unix::fs::PermissionsExt;
        let mode = fs::metadata(root).map_err(|_| JournalError::Io)?.permissions().mode();
        if mode & 0o077 != 0 {
            return Err(JournalError::InvalidRoot);
        }
    }
    Ok(())
}

fn create_lock(path: &Path) -> Result<File, JournalError> {
    let mut options = OpenOptions::new();
    options.write(true).create_new(true);
    #[cfg(unix)]
    {
        use std::os::unix::fs::OpenOptionsExt;
        options.mode(0o600);
    }
    let mut lock = options.open(path).map_err(|error| {
        if error.kind() == io::ErrorKind::AlreadyExists { JournalError::WriterBusy } else { JournalError::Io }
    })?;
    lock.write_all(b"heptabao-migration-writer-v1\n")
        .and_then(|()| lock.sync_all())
        .map_err(|_| JournalError::Io)?;
    Ok(lock)
}

fn open_journal(path: &Path) -> Result<File, JournalError> {
    let mut options = OpenOptions::new();
    options.read(true).write(true).create(true);
    #[cfg(unix)]
    {
        use std::os::unix::fs::OpenOptionsExt;
        options.mode(0o600);
    }
    options.open(path).map_err(|_| JournalError::Io)
}

pub fn digest_bytes(bytes: &[u8]) -> [u8; 32] {
    digest::digest(&digest::SHA256, bytes)
        .as_ref()
        .try_into()
        .expect("SHA-256 output is always 32 bytes")
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::time::{SystemTime, UNIX_EPOCH};

    fn root(name: &str) -> PathBuf {
        let nonce = SystemTime::now().duration_since(UNIX_EPOCH).unwrap().as_nanos();
        let root = std::env::temp_dir().join(format!("heptabao-migration-journal-{name}-{}-{nonce}", std::process::id()));
        #[cfg(unix)]
        {
            use std::os::unix::fs::DirBuilderExt;
            let mut builder = fs::DirBuilder::new();
            builder.mode(0o700);
            builder.create(&root).unwrap();
        }
        #[cfg(not(unix))]
        fs::create_dir(&root).unwrap();
        root
    }

    fn object() -> MigrationObjectId {
        MigrationObjectId::parse("secret/data/application").unwrap()
    }

    #[test]
    fn restart_recovers_verified_transaction_and_requires_reconciliation() {
        let root = root("restart");
        let key = [7; 32];
        let source = digest_bytes(b"source");
        let target = digest_bytes(b"target");
        {
            let mut journal = MigrationJournal::open(&root, key).unwrap();
            journal.begin([1; 16], object(), source).unwrap();
            journal.mark_staged(&object(), target).unwrap();
            journal.mark_verified(&object()).unwrap();
            journal.mark_commit_pending(&object()).unwrap();
        }
        let mut reopened = MigrationJournal::open(&root, key).unwrap();
        assert_eq!(reopened.latest(&object()).unwrap().phase, MigrationPhase::CommitPending);
        assert_eq!(
            reopened.reconcile_commit(&object(), ReconciliationProof { committed: true, observed_target_digest: digest_bytes(b"wrong") }),
            Err(JournalError::InvalidTransition)
        );
        let committed = reopened.reconcile_commit(&object(), ReconciliationProof { committed: true, observed_target_digest: target }).unwrap();
        assert_eq!(committed.phase, MigrationPhase::Committed);
        drop(reopened);
        fs::remove_dir_all(root).unwrap();
    }

    #[test]
    fn torn_tail_is_truncated_but_authenticated_corruption_is_rejected() {
        let root = root("tail");
        let key = [8; 32];
        {
            let mut journal = MigrationJournal::open(&root, key).unwrap();
            journal.begin([2; 16], object(), digest_bytes(b"source")).unwrap();
        }
        let path = root.join("migration.journal");
        let valid = fs::metadata(&path).unwrap().len();
        OpenOptions::new().append(true).open(&path).unwrap().write_all(&[0, 0, 1]).unwrap();
        let recovered = MigrationJournal::open(&root, key).unwrap();
        assert_eq!(fs::metadata(&path).unwrap().len(), valid);
        drop(recovered);
        let mut bytes = fs::read(&path).unwrap();
        let index = bytes.len() / 2;
        bytes[index] ^= 0x40;
        fs::write(&path, bytes).unwrap();
        assert!(matches!(MigrationJournal::open(&root, key), Err(JournalError::Tampered) | Err(JournalError::InvalidRecord)));
        let _ = fs::remove_file(root.join("writer.lock"));
        fs::remove_dir_all(root).unwrap();
    }

    #[test]
    fn single_writer_and_state_machine_are_fail_closed() {
        let root = root("writer");
        let key = [9; 32];
        let mut first = MigrationJournal::open(&root, key).unwrap();
        assert!(matches!(MigrationJournal::open(&root, key), Err(JournalError::WriterBusy)));
        first.begin([3; 16], object(), digest_bytes(b"source")).unwrap();
        assert_eq!(first.mark_verified(&object()), Err(JournalError::InvalidTransition));
        first.mark_rollback_pending(&object()).unwrap();
        assert_eq!(first.reconcile_rollback(&object(), false), Err(JournalError::InvalidTransition));
        first.reconcile_rollback(&object(), true).unwrap();
        drop(first);
        fs::remove_dir_all(root).unwrap();
    }

    #[test]
    fn duplicate_begin_is_idempotent_for_exact_operation() {
        let root = root("idempotent");
        let key = [10; 32];
        let mut journal = MigrationJournal::open(&root, key).unwrap();
        let first = journal.begin([4; 16], object(), digest_bytes(b"source")).unwrap();
        let second = journal.begin([4; 16], object(), digest_bytes(b"source")).unwrap();
        assert_eq!(first, second);
        assert_eq!(first.sequence, 1);
        drop(journal);
        fs::remove_dir_all(root).unwrap();
    }
}
''', encoding='utf-8')

          guide = Path('docs/modules/heptabao-migration-journal.md')
          sections = [
              ('Purpose and boundaries', 'Provides an authenticated, append-only migration transaction state machine. It does not itself claim full OpenBao object coverage or production migration authority.'),
              ('Public API and ownership', '`MigrationJournal` owns durable phase transitions; `MigrationObjectId`, `MigrationRecord`, and `ReconciliationProof` are the public contracts.'),
              ('State and data model', 'Records bind sequence, operation ID, object ID, source and target digests, phase, previous tag, and HMAC tag.'),
              ('Invariants and authorization', 'One writer is admitted per journal root. Commit after an uncertain outcome requires a matching target digest reconciliation proof.'),
              ('Failure, retry and reconciliation', 'Torn tails are truncated only after the authenticated prefix is verified. HMAC or chain failures are rejected. Exact duplicate begin requests are idempotent.'),
              ('Concurrency model', 'A create-new owner-private lock serializes writers. Readers are reconstructed from the authenticated journal under that lock.'),
              ('Security considerations', 'Roots reject symlink traversal and permissive Unix modes. Journal records form an HMAC chain and the in-memory key is overwritten on drop.'),
              ('Persistence and compatibility', 'The binary `HBMJ1` format is versioned and bounded. Format migration requires a future explicit decoder rather than silent reinterpretation.'),
              ('Observability', 'Typed phases and errors distinguish invalid transitions, writer contention, tampering, I/O failure, and outcome-unknown writes.'),
              ('Operations', 'Operators must preserve the journal and key together, resolve stale locks through an audited recovery procedure, and reconcile pending commits before retry.'),
              ('Tests and evidence', 'Tests cover restart recovery, reconciliation, torn-tail repair, authenticated corruption, single-writer exclusion, invalid transitions, rollback, and idempotency.'),
              ('Evolution and open boundaries', 'Adapters for every OpenBao mount, auth, identity, policy, lease, audit, and key format remain separate work. Independent cutover and rollback evidence is still required.'),
          ]
          body=['# heptabao-migration-journal','', 'Package: `heptabao-migration-journal`','', 'Source: `crates/heptabao-migration-journal`','']
          for title, text in sections:
              body += [f'## {title}','',text,'']
          guide.write_text('\n'.join(body), encoding='utf-8')

          test = Path('tests/repository/test_migration_journal_v2_4.py')
          test.write_text('''from pathlib import Path\nimport unittest\n\n\nclass MigrationJournalV24Tests(unittest.TestCase):\n    def test_authenticated_journal_and_reconciliation_are_materialized(self):\n        source = Path("crates/heptabao-migration-journal/src/lib.rs").read_text(encoding="utf-8")\n        for marker in ("HMAC_SHA256", "previous_tag", "CommitPending", "ReconciliationProof", "WriterBusy", "OutcomeUnknown"):\n            self.assertIn(marker, source)\n\n    def test_module_guide_declares_open_boundaries(self):\n        guide = Path("docs/modules/heptabao-migration-journal.md").read_text(encoding="utf-8")\n        self.assertIn("Independent cutover and rollback evidence is still required", guide)\n        self.assertIn("It does not itself claim full OpenBao object coverage", guide)\n\n\nif __name__ == "__main__":\n    unittest.main()\n''', encoding='utf-8')

          for file_name in ('README.md', 'docs/CURRENT_DOCUMENTATION.md', 'docs/modules/README.md'):
              path = Path(file_name)
              if not path.exists():
                  continue
              text = path.read_text(encoding='utf-8')
              text = re.sub(r'current workspace contains \*\*45 packages\*\*', 'current workspace contains **46 packages**', text, flags=re.I)
              text = re.sub(r'workspace contains \*\*45 packages\*\*', 'workspace contains **46 packages**', text, flags=re.I)
              if file_name == 'docs/modules/README.md' and 'heptabao-migration-journal.md' not in text:
                  text += '\n- [`heptabao-migration-journal`](./heptabao-migration-journal.md) — authenticated resumable migration transactions.\n'
              path.write_text(text, encoding='utf-8')
          PY

      - name: Remove write transport and qualify package
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/v24-migration-journal-materializer.yml
          rustup toolchain install 1.98.0 --profile minimal --component rustfmt --component clippy
          cargo +1.98.0 generate-lockfile
          cargo +1.98.0 fmt --all
          out="$RUNNER_TEMP/heptabao-v24-migration-journal"
          mkdir -p "$out"
          python -m unittest tests.repository.test_migration_journal_v2_4 2>&1 | tee "$out/structural-regression.log"
          python scripts/validate_repository_v2.py 2>&1 | tee "$out/repository-validator.log"
          python scripts/validate_compatibility_corpus.py 2>&1 | tee "$out/compatibility-validator.log"
          python -m unittest discover -s tests/repository -p 'test_*.py' 2>&1 | tee "$out/repository-tests.log"
          cargo +1.98.0 test --locked -p heptabao-migration-journal --all-targets 2>&1 | tee "$out/migration-journal-tests.log"
          cargo +1.98.0 fmt --all -- --check 2>&1 | tee "$out/fmt.log"
          timeout --signal=TERM --kill-after=30s 3600s cargo +1.98.0 test --workspace --all-targets --locked 2>&1 | tee "$out/workspace-tests.log"
          cargo +1.98.0 clippy --workspace --all-targets --locked -- -D warnings 2>&1 | tee "$out/clippy.log"
          cargo +1.98.0 doc --workspace --no-deps --locked 2>&1 | tee "$out/rustdoc.log"
          cargo +1.98.0 build --locked -p heptabao-server 2>&1 | tee "$out/server-build.log"

      - name: Publish source-only migration journal candidate
        shell: bash
        run: |
          set -euo pipefail
          test ! -e .github/workflows/v24-migration-journal-materializer.yml
          git config user.name 'HeptaBao V2.4 Migration Materializer'
          git config user.email '102159240+ProfHepta@users.noreply.github.com'
          git add -A
          git commit -m 'feat(migration): add authenticated resumable transaction journal'
          git push --force-with-lease="refs/heads/$OUTPUT_BRANCH:" origin "HEAD:refs/heads/$OUTPUT_BRANCH"

      - name: Upload bounded migration evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: heptabao-v24-migration-journal
          path: ${{ runner.temp }}/heptabao-v24-migration-journal/
          if-no-files-found: warn
          retention-days: 3
