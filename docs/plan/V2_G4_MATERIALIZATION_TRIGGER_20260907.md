# V2 G4 materialization trigger

This branch-only trigger exists to cause the already staged, digest-bound V2 overlay materializer to run. It grants no qualification, compatibility, migration, production, release, or external authority. The final materialized source must remove this trigger together with all transport chunks and must restore the active assurance workflow to read-only permissions.
