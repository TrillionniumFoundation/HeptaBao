# HeptaBao V2.1 execution status

This file is intentionally non-authoritative. Canonical capability and blocker truth remains in the machine-readable planning files.

The V2.1 branch targets `main`, carries the reviewed V2 source tree, adds the restart-safe `heptabao-durable-service` candidate, and keeps all legal, production, compatibility, migration, and release authority fail-closed pending authentic evidence.

For the current runnable assembly, see `docs/architecture/HEPTABAO_CURRENT_RUNTIME_ARCHITECTURE.md` and `docs/modules/CURRENT_RUNTIME_MAP.md`: the workspace has 46 packages; the runtime dependency closure is derived from Cargo manifests in the linked map. The server now composes persisted private auth/ACL, engines, authenticated audit rotation, maintenance and optional per-process HA. The earlier paragraph records the original durable-service increment, not the entire present implementation. Named historical test receipts remain in `docs/plan/HEPTABAO_SINGLE_NODE_EXECUTION_STATUS.md`; current source/guide digests and fresh validation are separate.

The current candidate adds an HA owner-publication binding: the owner manifest is preflighted and its operation and logical-state digests must match the exact state proposed to Raft before replication is attempted. Learner admission now uses a non-blocking OpenRaft membership commit followed by an explicit committed, non-joint, matched-frontier and recent-heartbeat observation; the destructive fixture starts a valid three-voter cluster before exercising a wrong-cluster restart. The current candidate passed the Linux guest workspace format check, 328 `heptabao-server` tests, the remaining workspace tests (including 96 vendored `qrcode` tests), and first-party Clippy with `--exclude qrcode`. The real three-process destructive fixture passed 23 scoped scenarios using binary SHA-256 `b800a641af9f64ac0a7c96395fec90e2b58ed52c73523fc76ad0af88c86df1f4`; the receipt remains scoped and leaves partition, power-loss, disk-full, forced snapshot install, rolling upgrade, membership change and longitudinal linearizability open.

The storage follow-up adds `large_unchanged_owner_bounds_v4_write_set_to_changed_owner` to the server integration suite. On the SSD Linux guest, the exact test run passed 329/329 server tests after constructing a multi-megabyte owner set and changing only the auth owner; the assertion requires unchanged owner resources to be reused and bounds the staged/retired mutation set to the changed owner's old/new chunks plus the manifest. This is local copy-on-write evidence only; HA still proposes the complete logical image and the scalable-state admission remains open.

The current `6cc53a3` Linux guest binary (`eac7867bc9f7b0626b291bce7362eec8eba3a61b02578549e17644e18c4aaf8f`) passed refreshed scoped HA receipts: [38 native membership scenarios](../../qa/openbao-acceptance/evidence/raft-membership-6cc53a3.json), [41 membership/dead-voter cleanup scenarios](../../qa/openbao-acceptance/evidence/raft-membership-dead-cleanup-6cc53a3.json), [network-partition scenarios](../../qa/openbao-acceptance/evidence/ha-network-partition-6cc53a3.json), [explicit step-down scenarios](../../qa/openbao-acceptance/evidence/ha-step-down-6cc53a3.json), and [idle lifecycle after leader loss](../../qa/openbao-acceptance/evidence/idle-lifecycle-ha-6cc53a3.json). These receipts cover loopback process isolation, replay-safe membership changes, learner catch-up, dead-voter grace/removal, partition fencing, leadership transfer, autonomous expiry and restart persistence. They remain repository-controlled scoped evidence and leave multi-host WAN, power loss, disk-full/permission faults, forced snapshot installation, mixed-version rolling upgrades, >32,000-operation longitudinal campaigns and independent reproduction open.

The HA readiness fence from product commit `7662519` was rebuilt after the owner-plan reuse increment `14dccd6` on the SSD Linux guest as binary `664b89ded689d2dd8029ed47714afcc0d6d9049f724289a1186eab98a1018010`. The [54-scenario replay-epoch receipt](../../qa/openbao-acceptance/evidence/replay-epoch-ha-664b89.json) covers three-process mTLS/Raft bootstrap, committed-application readiness, quorum loss, snapshot catch-up, leader failover and four replay-epoch retirements with an offline voter. Ordinary HA writes now reuse the preflight authenticated owner plan in the local atomic batch, removing duplicate five-owner serialization and chunk planning; HA still constructs the complete logical state for the HBSM3 manifest and remains bounded by the 16 MiB state limit. `ha_active` and successful health status now require the local state digest to match the committed Raft application envelope. This remains repository-controlled loopback evidence; multi-host WAN, power loss, disk-full/permission faults, rolling upgrade, longitudinal scale and independent reproduction remain open.

The clean `facced1` source was rebuilt on the SSD Linux guest and compared with the checksum-pinned OpenBao 2.6.2 arm64 oracle. The [current scoped differential receipt](../../qa/openbao-acceptance/evidence/openbao-2.6.2-comparison-facced1.json) reports `cases_match=true` with no mismatches for the selected bounded cases; the receipt binds candidate binary `63f209…` and the exact source head. It still leaves full endpoint error precedence, other auth/engines, persistent restart, HA, migration, upgrade, agent/proxy, and production security outside this comparison.

The current exact-head `079e2cb` Linux arm64 binary (`21586fb112c9fa38283f23f9e65ad3a68d67af7955e6d79bb09c42a195c58857`) also passed the [real OpenLDAP provider fixture](../../qa/openbao-acceptance/evidence/ldap-openldap-live-079e2cb.json): 18 checks covering a host-installed Ubuntu `slapd` distribution, CA-verified LDAPS bind, group search/policy projection, wrong-password denial, outage/restart recovery, membership revocation on the next login, service restart persistence and local-authority removal. The fixture runs only inside the dedicated SSD Lima guest; its short-lived slapd uses the guest's unconfined AppArmor test profile because Ubuntu's packaged profile rejects owner-only LMDB locks under the temporary fixture directory. This is scoped external-provider evidence, not independent qualification or full OpenBao LDAP field/error parity; privileged bind-account search, StartTLS/SASL/referrals, nested groups and broader external-provider gates remain open.

The same Linux arm64 binary passed the [provider-concurrency fixture](../../qa/openbao-acceptance/evidence/provider-concurrency-live-bcbc0fd.json): 13 checks held a real enrolled JWKS request open while an unrelated KV write and read completed (4.720 ms and 2.442 ms in this run), then verified login completion and the KV value after restart. This demonstrates local provider isolation and durable commit behavior only; it does not qualify multi-host provider faults, external provider independence, or full OpenBao parity.

The exact `45c7edc` Linux arm64 binary (`e389be313be0c5240a2dadd33cd866ae215e28f4c2f2ec7a95dba26de6b1e72a`) passed the real PostgreSQL 17.11 PGDG arm64 provider profile: [54 checks](../../qa/openbao-acceptance/evidence/postgresql-live-45c7edc.json) covering CA-verified TLS/SCRAM bootstrap, dynamic issue/renew/revoke, active-session termination, provider-fence blocking with unrelated KV progress, restart/outage reconciliation, more than 128 retired lifecycles with compact provider state, and idle-expiry role retirement. The run is repository-controlled scoped evidence only; static roles, root rotation, the full OpenBao statement/template/error surface, multi-host faults and independent qualification remain open.

The exact `e7e5a0f` candidate was also exercised against a checksum-pinned OpenBao 2.6.2 arm64 oracle. The bounded ACL policy adapter passed 19/19 live checks (dry-run isolation, exact two-policy readback, reserved `root`/`default` exclusion, restart and idempotent resume) and the bounded identity adapter passed 22/22 live checks (entity and internal-group recreation, membership-ID rewrite, real acknowledgement-loss recovery, restart idempotence and alias rejection without target effects). The sanitized receipts are [policy migration](../../qa/openbao-acceptance/evidence/policy-migration-e7e5a0f.json) and [identity migration](../../qa/openbao-acceptance/evidence/identity-migration-e7e5a0f.json). They remain scoped adapters: aliases, credentials, tokens, full-asset migration, cutover, rollback, independent qualification and production authority are false.
