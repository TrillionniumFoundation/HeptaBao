# heptabao-server module closure dossier

This dossier is the independently reviewable design, boundary, failure-semantics, and acceptance record for **`heptabao-server`**. It is generated from the exact candidate tree and must be reviewed whenever the source or manifest hash changes. It does not grant compatibility, production, migration, or release authority.

## Design and state ownership

- **Capability domain:** runnable TLS encrypted secrets service with bounded online Kubernetes/OIDC identity, root-only capacity observation and recovery-safe journal compaction
- **Repository state:** `IMPLEMENTED_REVIEW_REQUIRED`.
- **Source root:** `crates/heptabao-server`; Rust files: `crates/heptabao-server/src/audit_platform_tests.rs`, `crates/heptabao-server/src/audit_rotation.rs`, `crates/heptabao-server/src/auth.rs`, `crates/heptabao-server/src/auth_acl.rs`, `crates/heptabao-server/src/auth_capabilities.rs`, `crates/heptabao-server/src/auth_cubbyhole.rs`, `crates/heptabao-server/src/auth_cubbyhole_tests.rs`, `crates/heptabao-server/src/auth_identity.rs`, `crates/heptabao-server/src/auth_kubernetes.rs`, `crates/heptabao-server/src/auth_oidc.rs`, `crates/heptabao-server/src/auth_remote.rs`, `crates/heptabao-server/src/auth_tests.rs`, `crates/heptabao-server/src/auth_wrapping.rs`, `crates/heptabao-server/src/capabilities_service_tests.rs`, `crates/heptabao-server/src/crypto.rs`, `crates/heptabao-server/src/cubbyhole_service_tests.rs`, `crates/heptabao-server/src/engine_identity.rs`, `crates/heptabao-server/src/engine_leases.rs`, `crates/heptabao-server/src/engine_tests.rs`, `crates/heptabao-server/src/engines/identity.rs`, `crates/heptabao-server/src/engines/identity_runtime.rs`, `crates/heptabao-server/src/engines/kv.rs`, `crates/heptabao-server/src/engines/pki.rs`, `crates/heptabao-server/src/engines/ssh.rs`, `crates/heptabao-server/src/engines/totp.rs`, `crates/heptabao-server/src/engines/transit.rs`, `crates/heptabao-server/src/engines.rs`, `crates/heptabao-server/src/federated_auth.rs`, `crates/heptabao-server/src/ha.rs`, `crates/heptabao-server/src/ha_forward.rs`, `crates/heptabao-server/src/ha_state.rs`, `crates/heptabao-server/src/http.rs`, `crates/heptabao-server/src/identity_service_tests.rs`, `crates/heptabao-server/src/lib.rs`, `crates/heptabao-server/src/main.rs`, `crates/heptabao-server/src/outbound.rs`, `crates/heptabao-server/src/pki_service_tests.rs`, `crates/heptabao-server/src/postgres_wire.rs`, `crates/heptabao-server/src/service.rs`, `crates/heptabao-server/src/service_capabilities.rs`, `crates/heptabao-server/src/service_capacity.rs`, `crates/heptabao-server/src/service_capacity_tests.rs`, `crates/heptabao-server/src/service_database.rs`, `crates/heptabao-server/src/service_identity.rs`, `crates/heptabao-server/src/service_leases.rs`, `crates/heptabao-server/src/service_lifecycle.rs`, `crates/heptabao-server/src/service_online_auth.rs`, `crates/heptabao-server/src/service_raft_admin.rs`, `crates/heptabao-server/src/service_state_store.rs`, `crates/heptabao-server/src/service_state_store_integration_tests.rs`, `crates/heptabao-server/src/service_tests.rs`, `crates/heptabao-server/src/ssh_service_tests.rs`, `crates/heptabao-server/src/wrapping_service_tests.rs`.
- **Internal dependencies:** `heptabao-durable-service`, `heptabao-ha-service`, `heptabao-raft-runtime`.
- **Runtime placement:** `yes`. The current runtime map is authoritative for whether this package is in the executable server dependency closure.
- **Public design surface:** struct `AuditConfig`; struct `AuditRotation`; fn `open`; fn `active_path`; fn `config`; fn `recover`; fn `before_append`; struct `AuthState`; struct `Principal`; fn `is_root`; fn `consumed_use`; struct `AuthResponse`; struct `AuthError`; fn `bootstrap`; fn `authenticate`; fn `authorize_request`; fn `authorize_sudo_request`; fn `remount_mount`; fn `handle`; fn `parse_strict_json`; struct `LeaseIssuer`; fn `lease_issuer`; fn `lease_issuer_by_digest`; const `DEFAULT_RULES`; struct `Decision`; fn `consider`; fn `allowed`; struct `InspectionTarget`; fn `inspection_target`; fn `inspect_capabilities`

The module owns only the state and transitions described by its source files. It must not silently create an HTTP route, persistence format, authorization decision, external effect, or production guarantee unless that responsibility is visible in the source and in the current runtime map. Cross-module state is passed through typed APIs; callers remain responsible for transaction scope and durable publication where this package has no storage dependency.

## Module boundaries and trust assumptions

The package boundary is `crates/heptabao-server`. Inputs crossing it are untrusted unless the source validates them; secrets, credentials, bearer tokens, and provider responses must not be placed in logs or debug output. heptabao-server has no authority to claim OpenBao compatibility merely because a type or contract exists. Runtime integration, if any, is limited to the routes and owners recorded in `docs/modules/CURRENT_RUNTIME_MAP.md`; otherwise this is a standalone model, contract, or qualification tool.

The module does not own external clocks, network peers, KMS/HSM custody, filesystem ownership, process supervision, or operator approval unless its source explicitly implements and tests that boundary. Those concerns remain caller obligations and are listed as open evidence below.

## Failure semantics and ordering

The source-defined failure vocabulary is: `AuthError::MalformedToken`; `AuthError::DuplicateJsonKey`; `AuthError::InvalidJson`; `AuthError::MissingClaim`; `AuthError::InvalidClaim`; `AuthError::InvalidPolicy`; `AuthError::InvalidKey`; `AuthError::DuplicateKeyId`; `AuthError::UnknownKeyId`; `AuthError::AlgorithmDenied`; `AuthError::SignatureInvalid`; `AuthError::ClaimDenied`; `AuthError::TokenTimeInvalid`; `AuthError::ReplayDetected`; `AuthError::ReplayRecordInvalid`; `AuthError::InvalidMfaProof`; `AuthError::WriterBusy`; `AuthError::Tampered`; `AuthError::CapacityExceeded`; `AuthError::OutcomeUnknown`; `ReplicatedStateError::InvalidKey`; `ReplicatedStateError::InvalidCluster`; `ReplicatedStateError::InvalidState`; `ReplicatedStateError::InvalidEnvelope`; `ReplicatedStateError::BaseStateConflict`; `ReplicatedStateError::AuthenticationFailed`; `ReplicatedStateError::DigestMismatch`; `ReplicatedStateError::RandomnessUnavailable`; `StateStoreError::EmptyState`; `StateStoreError::StateTooLarge`; `StateStoreError::InvalidOperationId`; `StateStoreError::InvalidManifest`; `StateStoreError::InvalidChunk`; `StateStoreError::DigestMismatch`; `StateStoreError::Serialization`

Validation must happen before irreversible state mutation. A caller must distinguish a definite pre-entry rejection from an outcome that became unknown after provider, journal, network, or publication entry. Unknown-after-entry outcomes require authoritative readback/reconciliation and must not be retried blindly. Invalid transitions, stale generations/terms, malformed identifiers, unauthorized inputs, exhausted capacity, and I/O/transport errors remain failures unless the source explicitly converts them into a typed safe state. This dossier does not reinterpret a missing error enum as success.

Ordering obligations are source-specific: inspect the public functions and tests listed below before changing call order. If this module is later given durable or external effects, add a persisted intent/readback test and update this dossier rather than relying on a happy-path unit test.

## Acceptance evidence

- **Source/manifest evidence:** portable repository-relative source SHA-256 `ad61c1f6917b8c5f8dcf15e47167fe927e07f661c69e8c5e8caec000e514d549`; manifest SHA-256 `d21014fe016940b364b759de364eaebc8865dbdbdd8ba5cee2daf3e3701e2f97`.
- **Named executable anchor:** `target_abi_opens_private_regular_files_and_real_directories` in `crates/heptabao-server/src/audit_platform_tests.rs`.
- **Required command:** `cargo +1.98.0 test --locked -p heptabao-server` (must be executed against this exact source tree; historical CI output is not current evidence).
- **Repository/documentation checks:** `python scripts/validate_module_closure.py`; `python scripts/validate_current_documentation_semantics.py`.
- **Acceptance interpretation:** a passing unit test proves only the named module behavior. It does not prove server integration, OpenBao parity, HA, external provider correctness, crash recovery, or production qualification. Those require separate executable profiles and independent admission.

The acceptance status for this dossier is **source-bound, execution-pending** until the exact-head command and applicable integration profile produce a receipt bound to the same commit.

## Known gaps and evolution

Current open boundaries include full API/error-surface review, adversarial and crash/reopen cases, platform qualification, and any integration claimed by a different package. When behavior changes, update this dossier, the module guide, capability matrix, runtime map and named tests together. Never replace an unexecuted or failed acceptance result with prose claiming completion.
